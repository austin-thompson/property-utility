# Cycles

ROADMAP defines stable project phases. Cycle documents represent actual implementation slices for the current repository state.

Each cycle should include:

- objective
- scope
- non-goals
- gap analysis where relevant
- ordered work items
- status
- acceptance criteria
- follow-up items
